import PropTypes from 'prop-types'

const XmasBorderPropTypes = {
	showOnDesktop: PropTypes.bool, // Should render on desktop
	showOnMobile: PropTypes.bool, // Should render on mobile
}

export default XmasBorderPropTypes
